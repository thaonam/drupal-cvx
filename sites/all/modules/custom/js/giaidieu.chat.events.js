/* custom chat events js created giaidieu */
//var socket = io.connect('http://thietkewebgiaidieu.com:8888');

var storage;

(function($){
	Drupal.behaviors.giaidieu_chat_init = {
		attach: function(context, settings) {

      //var storage = $.localStorage;

      // Setup channel update.
      //giaidieu_channel_update();

      // Auto update timestamp (ago).
      //giaidieu_timestamp_update();
    }
  };

})(jQuery);